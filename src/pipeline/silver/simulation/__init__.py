"""Backward-compatible silver simulation package.

Simulation implementations were moved under ``src.pipeline.gold.simulation``.
Legacy imports from ``src.pipeline.silver.simulation`` still exist in tests and
callers, so this package keeps those module paths available.
"""
